# Simple JSON Grammar for Runic PEG Parser

json := value

value := object | array | string | number | "true" | "false" | "null"

object := "{" members? "}"
members := pair ("," pair)*
pair := string ":" value

array := "[" elements? "]"
elements := value ("," value)*

string := STRING
number := INTEGER | FLOAT
